[project]
name = "portfolio-backend"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
    "python-dotenv",
    "google-genai",
]

[project.scripts]
app = "app.main:app"
